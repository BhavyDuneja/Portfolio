# Serve the main Anantasutra website at root
/ /index.html 200

# Handle static assets
/_next/* /_next/:splat 200
/images/* /images/:splat 200

# Fallback for any other routes
/* /index.html 200
